# simple-eCommerce-backend

This is a simple e-commerce app built with Node.js and connected with mongodb

All CRUD operations can be used by the admin dashboard.

You should replace the mongoDB uri in (app.js) with your uri.

you can try mail service with mailgun by adding your Domain and API key in (emailService.js) file in (util) folder.
